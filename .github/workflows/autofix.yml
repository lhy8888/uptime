name: autofix.ci

on:
  workflow_dispatch:
permissions: {}

jobs:
  autofix:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - run: echo "autofix.ci app is not installed for this repository. Use local lint/format commands or install the autofix.ci app before enabling this workflow on push/PR events."
